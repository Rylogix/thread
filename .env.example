# THREAD needs no environment variables for local browser development.
#
# Keep local credentials in .env.local or .dev.vars; both are ignored.
# Store production Worker secrets with `wrangler secret put`, never here or
# in wrangler.jsonc. The current application requires no production secrets.
